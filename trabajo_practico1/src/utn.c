/*
 * utn.c
 *
 *  Created on: 9 sept. 2020
 *      Author: Pescar
 */
// #include <stdio.h>
// #include <stdlib.h>
 	// AUTOCOMPLETADO: ctrl + barra espaciadora
	/*int utn_getCaracter(char* pResultado, char* mensaje, char* mensajeError, char minimo, char maximo, int reintentos);
	{
		do
		{
			printf("%s",mensaje);
			}
			scanf("%c",&buffer);
			if(buffer >= minimo && buffer <= maximo)
			{
				*pResultado = buffer;
				retorno = 0;
				break;
			}
			else
			{
				printf("%s",mensajeError);
				reintentos--;
			}
		}while(reintentos >= 0);

	}
	return retorno;*/
